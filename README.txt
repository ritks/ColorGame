Additional Features

- make game harder
- show stats at game end

Quality Changes
- black background
- each circle should have a small "shine" effect
- when the wront tile on a given row is selected, "disappear" just the incorrectly picked tile should be blended into the same as the background color
- when the correct tile on a given row is selected, "disappear" that whole row by blending it into the same as the background color
- 

TODOs
- get all 3 strikes
